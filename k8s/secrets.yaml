apiVersion: v1
kind: Secret
metadata:
  name: pdf-worker-secrets
  namespace: pdf-processing
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded values
  # To encode: echo -n "your-value" | base64
  GEMINI_API_KEY: ""  # Replace with base64 encoded API key
  GOOGLE_APPLICATION_CREDENTIALS: ""  # Replace with base64 encoded service account JSON
  GOOGLE_DRIVE_CREDENTIALS: ""  # Replace with base64 encoded OAuth2 credentials
  GCS_BUCKET_NAME: ""  # Replace with base64 encoded bucket name
  GCS_SOURCE_PREFIX: ""  # Replace with base64 encoded source prefix
  GCS_DEST_PREFIX: ""  # Replace with base64 encoded dest prefix
  DRIVE_SOURCE_FOLDER_ID: ""  # Replace with base64 encoded folder ID
  DRIVE_DEST_FOLDER_ID: ""  # Replace with base64 encoded folder ID
  SUPABASE_URL: ""  # Replace with base64 encoded Supabase URL
  SUPABASE_API_KEY: ""  # Replace with base64 encoded Supabase API key
